spring:
  datasource:
    url: jdbc:h2:mem:efgen_bosh_test;MODE=PostgreSQL;DB_CLOSE_DELAY=-1
    username: sa
    password:
  flyway:
    enabled: false
  jpa:
    hibernate:
      ddl-auto: none

logging:
  level:
    root: WARN
    com.efgenbosh: INFO
