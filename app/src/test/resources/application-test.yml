spring:
  datasource:
    url: jdbc:h2:mem:efgen_bosh_test;MODE=PostgreSQL;DB_CLOSE_DELAY=-1
    username: sa
    password:
  flyway:
    enabled: false
  jpa:
    hibernate:
      ddl-auto: none

logging:
  level:
    root: WARN
    com.efgenbosh: INFO

JWT_SECRET: 0123456789abcdef0123456789abcdef
JWT_EXPIRES_MINUTES: 30
JWT_REFRESH_EXPIRES_DAYS: 30
